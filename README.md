# python-journey
git init
git config --global user.name "Prasanna"
git config --global user.email "prasannasuragani@gmail.com"
git remote add origin https://github.com/Prasanna/python-journey.git
git pull origin main --allow-unrelated-histories
git add .
git commit -m "Day 1: environment setup, first Python programs, rule-based AI"
git branch -M main
git push -u origin main
